# Rice-Leaf-Diseases-Detection
Using a small rice leaf dataset, I used different machine learning techniques and build different convolutional neural networks to better classify which leaves had
what disease.

The repository has the final notebook code along with the small dataset.
